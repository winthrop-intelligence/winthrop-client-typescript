
# AthleticProfileShowSportFinancialsCostBuildHeadCoachUndisclosed

Private schools only — the sitting head coach when the IRS 990 prices no seat for them (the filing lists only the highest-paid officers and employees). Null whenever head_coach is priced, or for a public school.

## Properties

Name | Type
------------ | -------------
`name` | string
`lastName` | string

## Example

```typescript
import type { AthleticProfileShowSportFinancialsCostBuildHeadCoachUndisclosed } from '@winthrop-intelligence/winthrop-client-typescript'

// TODO: Update the object below with actual values
const example = {
  "name": null,
  "lastName": null,
} satisfies AthleticProfileShowSportFinancialsCostBuildHeadCoachUndisclosed

console.log(example)

// Convert the instance to a JSON string
const exampleJSON: string = JSON.stringify(example)
console.log(exampleJSON)

// Parse the JSON string back to an object
const exampleParsed = JSON.parse(exampleJSON) as AthleticProfileShowSportFinancialsCostBuildHeadCoachUndisclosed
console.log(exampleParsed)
```

[[Back to top]](#) [[Back to API list]](../README.md#api-endpoints) [[Back to Model list]](../README.md#models) [[Back to README]](../README.md)


